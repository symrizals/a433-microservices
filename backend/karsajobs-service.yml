apiVersion: v1
kind: Service
metadata:
  name: karsajobs-service
  labels:
    app: karsajobs
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: karsajobs
    tier: backend
