apiVersion: apps/v1
kind: Deployment
metadata:
  name: karsajobs
  labels:
    app: karsajobs
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: karsajobs
      tier: backend
  template:
    metadata:
      labels:
        app: karsajobs
        tier: backend
    spec:
      containers:
      - name: karsajobs
        image: symrizals/karsajobs:latest
        ports:
        - containerPort: 8080
        env:
        # Port aplikasi berjalan
        - name: APP_PORT
          value: "8080"
        # Host MongoDB diambil dari ConfigMap
        - name: MONGO_HOST
          value: mongo-service
        # Username MongoDB diambil dari Secret
        - name: MONGO_USER
          valueFrom:
            secretKeyRef:
              name: mongo-secret
              key: MONGO_ROOT_USERNAME
        # Password MongoDB diambil dari Secret
        - name: MONGO_PASS
          valueFrom:
            secretKeyRef:
              name: mongo-secret
              key: MONGO_ROOT_PASSWORD
