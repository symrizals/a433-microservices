# Service untuk expose Shipping Service
apiVersion: v1
kind: Service
metadata:
  name: shipping-service
spec:
  type: NodePort
  selector:
    app: shipping-service
  ports:
  - port: 3001
    targetPort: 3001
    nodePort: 30001
